function Header(){
  //header data. Left the same as before
    return(
        <header className="App-header">
          <h1 className="App-title">Gallery of My Life</h1>
        </header>
    )
};

export default Header